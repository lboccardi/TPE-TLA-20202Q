S{
	 i$i=1. i$t1=0. i$t2=0. i$next. 

	 P[Enter the number of terms: ].

	 G(n,10).

	 P[Fibonacci sequence: ]. 

	 (i<=n)?{
	 	i=i+1.
	 	P['t1' ].
	 	next = t1+t2. 
	 	t1=t2.
	 	t2=next.
	 }

	 R 0.

}